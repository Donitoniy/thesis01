# 智慧编程 - 中小学编程与AI教学辅助平台

一个面向中小学生的编程和AI教学辅助工具，采用简洁的设计理念，提供完整的学习和作业管理功能。

## 🌟 功能特色

### 四大主要模块

1. **编程学习**
   - 编程课程：系统化的编程学习内容
   - 代码练习：丰富的编程练习题库

2. **AI教学**
   - AI基础知识：人工智能概念和原理
   - AI工具体验：实际体验各种AI工具

3. **作业管理**
   - 作业提交：便捷的作业上传系统
   - 作业批改：智能批改和反馈系统

4. **个人中心**
   - 学习进度：可视化学习进度跟踪
   - 个人设置：自定义学习偏好

## 🛠️ 技术栈

- **前端**: 原生 HTML + CSS + JavaScript
- **后端服务**: Firebase
  - Firebase Auth (用户认证)
  - Firestore (数据库)
  - Firebase Hosting (网站托管)
  - Firebase Storage (文件存储，可选)

## 🚀 快速开始

### 1. 克隆项目
```bash
git clone [项目地址]
cd yy网站设计
```

### 2. 配置 Firebase

#### 2.1 创建 Firebase 项目
1. 访问 [Firebase Console](https://console.firebase.google.com/)
2. 点击"创建项目"
3. 输入项目名称，例如："mypg"-smart-programming123
4. 完成项目创建

#### 2.2 启用必要服务

**Authentication (认证)**
1. 在左侧菜单选择 "Authentication"
2. 点击 "开始使用"
3. 在 "Sign-in method" 标签页启用 "电子邮件地址/密码"

**Firestore Database (数据库)**
1. 在左侧菜单选择 "Firestore Database"
2. 点击 "创建数据库"
3. 选择 "以测试模式启动"（稍后会设置安全规则）

**Hosting (托管)**
1. 在左侧菜单选择 "Hosting"
2. 点击 "开始使用"
3. 按照指引完成设置

#### 2.3 获取配置信息
1. 在项目设置中找到 "您的应用" 部分
2. 点击 "添加应用" > "Web"
3. 注册应用并复制配置代码
4. 将配置信息替换到 `firebase-config.js` 文件中

### 3. 设置安全规则

#### Firestore 安全规则
在 Firestore Database > 规则 中设置：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 允许已认证用户读写自己的作业
    match /homework/{document} {
      allow read, write: if request.auth != null && 
        request.auth.uid == resource.data.studentId;
    }
    
    // 允许已认证用户创建作业
    match /homework/{document} {
      allow create: if request.auth != null && 
        request.auth.uid == request.resource.data.studentId;
    }
  }
}
```

### 4. 本地测试

直接在浏览器中打开 `index.html` 文件即可进行本地测试。

### 5. 部署到 Firebase Hosting

#### 5.1 安装 Firebase CLI
```bash
npm install -g firebase-tools
```

#### 5.2 登录并初始化
```bash
firebase login
firebase init hosting
```

#### 5.3 部署
```bash
firebase deploy
```

## 📁 项目结构

```
yy网站设计/
├── index.html          # 主页面
├── styles.css          # 样式文件
├── script.js           # 主要JavaScript逻辑
├── firebase-config.js  # Firebase配置
└── README.md          # 项目说明
```

## 🎨 设计特色

- **简洁美观**: 采用现代化的渐变色设计和卡片布局
- **响应式**: 完美适配桌面端和移动端
- **用户友好**: 直观的导航和清晰的功能分区
- **动画效果**: 流畅的过渡动画提升用户体验

## 🔧 自定义配置

### 修改主题色彩
在 `styles.css` 中修改以下CSS变量：
```css
:root {
  --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --primary-color: #667eea;
  --secondary-color: #764ba2;
}
```

### 添加新功能
1. 在对应的HTML section中添加新的功能卡片
2. 在 `script.js` 的 `handleFeatureClick` 函数中添加处理逻辑
3. 根据需要在Firestore中创建新的数据集合

## 📱 浏览器兼容性

- Chrome 60+
- Firefox 55+
- Safari 12+
- Edge 79+
