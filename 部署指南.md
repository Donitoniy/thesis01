# 智慧编程平台 - 快速部署指南

## 一键部署到Firebase Hosting

### 步骤1：创建Firebase项目
1. 访问 [Firebase Console](https://console.firebase.google.com/)
2. 点击"创建项目"
3. 输入项目名称："智慧编程平台"
4. 完成项目创建

### 步骤2：启用必要服务
1. **Authentication**: 启用邮箱/密码登录
2. **Firestore Database**: 创建数据库（测试模式）
3. **Hosting**: 启用网站托管

### 步骤3：获取配置信息
1. 项目设置 → 您的应用 → 添加Web应用
2. 复制配置代码
3. 替换 `firebase-config.js` 中的配置信息

### 步骤4：部署网站

#### 环境要求
- **Node.js**: v16.18.0 或更高版本
- **npm**: v8.15.0 或更高版本

#### 部署命令
```bash
# 安装Firebase CLI（兼容Node.js v16）
npm install -g firebase-tools@11.30.0

# 登录Firebase
firebase login
# 如果遇到网络问题，可尝试：
# firebase login --no-localhost

# 在项目目录中初始化
firebase init hosting

# 部署网站
firebase deploy
```

#### 版本兼容性说明
- **Node.js v16**: 使用 `firebase-tools@11.30.0`
- **Node.js v18+**: 可使用最新版 `firebase-tools`
- **Node.js v20+**: 推荐使用最新版 `firebase-tools`

## 故障排除

### 常见问题及解决方案

#### 1. Firebase CLI版本不兼容
**错误**: `Firebase CLI v14.x.x is incompatible with Node.js v16.x.x`
**解决**: 安装兼容版本
```bash
npm install -g firebase-tools@11.30.0
```

#### 2. 登录失败
**错误**: `Failed to make request to https://auth.firebase.tools/attest`
**解决**: 尝试以下方法
```bash
# 方法1：使用no-localhost选项
firebase login --no-localhost

# 方法2：检查网络连接和防火墙设置
# 方法3：使用CI token登录
firebase login:ci
```

#### 3. Firebase模块导入错误
**错误**: `TypeError: Failed to resolve module specifier "firebase/app"`
**解决**: 已使用CDN版本，确保网络连接正常

#### 4. 认证功能不工作
**错误**: `演示模式：登录功能需要配置Firebase`
**解决**: 检查firebase-config.js中的配置是否正确

## 本地测试
```bash
# 启动本地服务器
python -m http.server 8000

# 访问 http://localhost:8000
```

## 🔧 配置说明

### Firebase配置文件位置
- `firebase-config.js` - 主配置文件
- `firestore.rules` - 数据库安全规则
- `firebase.json` - 部署配置
